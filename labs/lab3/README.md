# Лабораторная работа №3: Межпроцессное взаимодействие через Shared Memory

## 📋 О работе
Реализация системы межпроцессного взаимодействия между родительским и дочерним процессами с использованием технологии shared memory (memory-mapped files). Программа вычисляет суммы чисел из входного файла, распределяя обработку между процессами.

## 🎯 Цель работы
Приобретение практических навыков в:
- Управление процессами в ОС
- Обеспечение обмена данных между процессами посредством технологии "File mapping"
- Работа с файловыми системами через memory-mapped files

## 📝 Задание
Составить программу на языке Си, осуществляющую работу с процессами и взаимодействие между ними. В результате работы программа (основной процесс) должен создать для решения задачи один дочерний процесс. Взаимодействие между процессами осуществляется через отображаемые файлы (memory-mapped files).

**Конкретная задача**: В файле записаны команды вида: «число число число<endline>». Дочерний процесс вычисляет их суммы и выводит результат. Числа имеют тип int. Количество чисел может быть произвольным.

## ⚙️ Требования к системе
- Компилятор GCC
- Операционная система Linux/WSL
- Поддержка POSIX Shared Memory (-lrt)
- 4+ GB оперативной памяти

## 🛠️ Использованные технологии
- **Язык программирования**: C (стандарт C99)
- **Межпроцессное взаимодействие**: POSIX Shared Memory
- **Синхронизация**: флаги готовности данных (data_ready, process_complete)
- **Управление процессами**: fork(), waitpid()
- **Файловые операции**: fopen(), fread(), fclose()
- **Сборка**: Makefile

## 🏗️ Архитектура решения

### Структура shared memory:
```c
typedef struct {
    char data[SHARED_SIZE];      // Данные для обработки
    int data_ready;              // Флаг готовности данных от родителя
    int process_complete;        // Флаг завершения обработки ребёнком
} shared_memory;
```

### Взаимодействие процессов:
1. **Родительский процесс**:
   - Получает имя файла от пользователя
   - Создает два сегмента shared memory
   - Загружает данные из файла в input_shm
   - Устанавливает флаг data_ready = 1
   - Ожидает установки флага process_complete
   - Выводит результаты и завершает работу

2. **Дочерний процесс**:
   - Ожидает data_ready = 1
   - Обрабатывает данные построчно, вычисляя суммы
   - Записывает результаты в output_shm
   - Устанавливает process_complete = 1

## 🚀 Запуск и использование

### Компиляция:
```bash
make
```

### Основные команды:
```bash
# Основной запуск
./parent

# Запуск с отладочной информацией
make debug

# Запуск под strace для отладки
make strace-parent
make strace-both

# Проверка shared memory объектов
make check-shm

# Автоматическое тестирование
make test
```

### Пример использования:
```bash
# Создание тестового файла
echo "1 2 3" > input.txt
echo "4 5 6" >> input.txt
echo "10 20 30" >> input.txt

# Запуск программы
./parent
Введите имя файла: input.txt
Результат обработки:
6
15
60
Обработка завершена
```

## 📊 Структура проекта

```
lab3/
├── shared_memory.h     # Заголовочный файл с структурами и прототипами
├── shared_memory.c     # Реализация работы с shared memory
├── parent.c            # Родительский процесс
├── child.c             # Дочерний процесс
├── Makefile            # Система сборки
└── README.md           # Документация
```

## 🔧 Ключевые функции

### Управление shared memory:
- `create_shared_memory()` - создает и настраивает сегмент shared memory
- `open_shared_memory()` - открывает существующий сегмент
- `close_shared_memory()` - корректно закрывает и освобождает ресурсы

### Обработка данных:
- **Родитель**: чтение файла, загрузка в shared memory, ожидание результатов
- **Дочерний**: парсинг строк, вычисление сумм, запись результатов

## ⚠️ Обработка ошибок

Программа включает комплексную обработку ошибок:
- Проверка возвращаемых значений системных вызовов
- Корректное освобождение ресурсов при ошибках
- Валидация входных параметров
- Обработка ситуаций с несуществующими файлами

## 🔍 Отладка и мониторинг

### Команды для отладки:
```bash
# Просмотр shared memory объектов
ls -la /dev/shm/

# Мониторинг процессов
ps aux | grep -E "(parent|child)"

# Очистка shared memory при аварийном завершении
make clean-all
```

### Встроенные цели Makefile:
- `make view-strace` - просмотр логов strace
- `make check-shm` - проверка состояния shared memory
- `make clean-all` - полная очистка (включая shared memory)

## Вывод
В ходе выполнения работы я освоил работу с разделяемой памятью (shared memory) в ОС Linux. Реализовал межпроцессное взаимодействие между родительским и дочерним процессом через именованные сегменты памяти.

### Что я узнал:

1. **Размер страницы памяти** – с помощью `getconf PAGESIZE` узнал, что стандартный размер страницы в системе составляет 4096 байт. Это аппаратно-оптимизированное значение для эффективной работы **MMU** - Memory Management Unit.

2. **Создание и управление shared memory**:
   - `shm_open()` – для создания/открытия именованного объекта разделяемой памяти (фактически файла в `/dev/shm/`).
   - `ftruncate()` – для выделения нужного объёма памяти под сегмент. Узнал, что `ftruncate64()` требуется только для файлов >2 ГБ на 32-битных системах.
   - `mmap()` – для отображения shared memory в адресное пространство процесса.

3. **Синхронизация процессов** – реализовал простой механизм синхронизации с помощью флагов `data_ready` и `process_complete`, избегая активного ожидания с помощью `usleep()`.

4. **Управление памятью** – освоил корректное освобождение ресурсов:
   - `munmap()` – для отмены отображения.
   - `shm_unlink()` – для удаления именованного объекта (только из родительского процесса).

### Архитектура клиент-серверного взаимодействия
   - Родительский процесс загружает данные из файла в shared memory.
   - Дочерний процесс обрабатывает данные (вычисляет суммы чисел в строках).
   - Результат возвращается в другой сегмент shared memory.

**Итог:** Работа позволила понять механизм разделяемой памяти на уровне системных вызовов POSIX, получить практический опыт межпроцессного взаимодействия и осознать важность корректного управления ресурсами и синхронизации в многопроцессных приложениях.

---
*Лабораторная работа выполнена в рамках курса "Операционные системы"*